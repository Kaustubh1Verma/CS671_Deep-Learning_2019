Report is all combined.

Data used for line dataset for 96 classes and multihead classification can be taken out from the respective folders.

For visualization of question 2,change name of line_dataset.npz to outfile.npz and place it in As2Q2 folder.

Model used are pretrained ones(as .h5 files), and are not placed in this submission due to their large size.
Only one model names as my_model.h5 trained on mnist dataset is placed and can be used.
